# 变更记录

本项目采用 `cloudflared版本-zh-cn.修订号` 的版本格式。

## 2026.8.2-zh-cn.8

- 默认账号和密码首次登录后必须更换，完成前不会开放隧道管理接口。
- 默认信息只允许在可信局域网内首次使用，避免公网抢先改密接管。
- 新增服务端会话吊销，退出登录后旧 Cookie 立即失效。
- 写入接口只接受同源 JSON 请求，补齐 CSP、防嵌入、禁缓存等安全响应头。
- 登录密码校验改为异步处理，并限制同时校验数量，避免阻塞管理服务。
- 增加全局请求频率限制，避免管理接口被持续占用。
- Tunnel Token 和 cloudflared 配置改为 `0600` 权限原子写入。
- 修正版本检查来源，改为读取本项目 GitHub 最新发布并增加超时和缓存。
- 容器构建排除本地配置和密钥文件；发布依赖固定到提交版本。
- Node.js 基础镜像固定到多架构摘要，示例部署启用只读根文件系统与 `no-new-privileges`。
- 新增源码审计、前端构建、镜像 SBOM、构建证明和高危漏洞扫描。
- 三种架构按镜像摘要分别扫描，正式标签只合并已通过扫描的原始摘要。

## 2026.8.2-zh-cn.7

- 登录页面的账号和密码输入框默认保持空白。
- 登录面板在浏览器可用区域内水平、垂直居中。
- 默认登录账号调整为 `admin`，默认密码调整为 `123456789`。
- 新增登录账号和密码修改功能。
- 修改后的密码使用 `scrypt` 加盐哈希持久化保存。
- 修改登录信息后，原有登录会话立即失效。
- 更新部署说明、默认登录信息和页面截图。

## 2026.8.2-zh-cn.6

- 新增中文登录页面。
- 新增签名会话 Cookie、登录失败限流和退出登录功能。
- 未登录请求无法访问隧道配置、状态和控制接口。
- 增加登录相关环境变量和正式部署说明。
- 使用多阶段容器构建和锁定依赖版本，提升构建一致性。
- 更新前后端依赖并完成安全检查。
- 更新登录页面与隧道管理页面截图。

## 2026.8.2-zh-cn.5

- 修正仓库首页错误引用上游英文截图的问题。
- 统一界面、源码、镜像和文档中的版本号。

## 2026.8.2-zh-cn.4

- 首次公开发布中文定制版本。
- 集成 cloudflared 2026.8.2。
- 支持 `linux/amd64`、`linux/arm64` 和 `linux/arm/v7` 镜像。
