# 变更记录

本项目采用 `cloudflared版本-zh-cn.修订号` 的版本格式。

## 2026.8.2-zh-cn.11

- 默认将单个 cloudflared 实例的 Cloudflare 边缘连接固定为 `1` 条，避免管理端看到两条边缘连接。
- 管理页面“连接详情”新增边缘连接数，实际启动参数和页面显示保持一致。
- 更新服务会保留 `HA_CONNECTIONS` 配置，后续一键更新不会恢复为多连接。
- 对边缘连接数进行范围校验，仅接受 cloudflared 支持的 `1` 至 `4`。

## 2026.8.2-zh-cn.10

- 新增管理页面一键更新，自动核对 GitHub 最新公开正式版本。
- 中文管理界面和 cloudflared 内核作为同一正式镜像一起更新，并在流水线和下载后进行版本一致性检查。
- 新增独立更新服务；Web 管理容器不挂载 Docker Socket，两者只通过本机 Unix Socket 和短时效 HMAC 签名通信。
- 更新目标固定为本项目正式镜像，拒绝任意仓库、任意标签、版本降级和任意容器操作。
- 下载新镜像期间保持原隧道运行，切换前在 `/www/临时文件` 保存不含密钥的更新记录并保留旧容器。
- 新版本启动检查失败时自动恢复旧容器，避免更新失败后管理页面长期离线。
- 新增更新接口频率限制、请求防重放、固定挂载目录检查和更新模块测试。
- 正式标签通过全部架构检查并发布镜像后，流水线自动创建 GitHub Release，确保页面能及时发现新版本。

## 2026.8.2-zh-cn.9

- 运行镜像改用固定摘要的 Distroless Node.js，仅保留管理服务运行所需文件。
- cloudflared 继续保持 `2026.8.2`，并使用带安全修复的 Go `1.26.6` 从官方标签源码重新构建。
- cloudflared 官方源码归档、Go 构建镜像和运行镜像均固定摘要或 SHA-256 校验值。
- 移除运行时不再需要的 npm、curl、包管理器和命令查找依赖，进一步缩小攻击面。
- 镜像扫描同时保留完整报告，并阻止存在可修复高危或严重漏洞的镜像发布。
- 修正三种架构镜像扫描参数，单个架构失败时仍会完成其余架构的审查。

## 2026.8.2-zh-cn.8

- 默认账号和密码首次登录后必须更换，完成前不会开放隧道管理接口。
- 默认信息只允许在可信局域网内首次使用，避免公网抢先改密接管。
- 新增服务端会话吊销，退出登录后旧 Cookie 立即失效。
- 写入接口只接受同源 JSON 请求，补齐 CSP、防嵌入、禁缓存等安全响应头。
- 登录密码校验改为异步处理，并限制同时校验数量，避免阻塞管理服务。
- 增加全局请求频率限制，避免管理接口被持续占用。
- Tunnel Token 和 cloudflared 配置改为 `0600` 权限原子写入。
- 修正版本检查来源，改为读取本项目 GitHub 最新发布并增加超时和缓存。
- 容器构建排除本地配置和密钥文件；发布依赖固定到提交版本。
- Node.js 基础镜像固定到多架构摘要，示例部署启用只读根文件系统与 `no-new-privileges`。
- 新增源码审计、前端构建、镜像 SBOM、构建证明和高危漏洞扫描。
- 三种架构按镜像摘要分别扫描，正式标签只合并已通过扫描的原始摘要。

## 2026.8.2-zh-cn.7

- 登录页面的账号和密码输入框默认保持空白。
- 登录面板在浏览器可用区域内水平、垂直居中。
- 默认登录账号调整为 `admin`，默认密码调整为 `123456789`。
- 新增登录账号和密码修改功能。
- 修改后的密码使用 `scrypt` 加盐哈希持久化保存。
- 修改登录信息后，原有登录会话立即失效。
- 更新部署说明、默认登录信息和页面截图。

## 2026.8.2-zh-cn.6

- 新增中文登录页面。
- 新增签名会话 Cookie、登录失败限流和退出登录功能。
- 未登录请求无法访问隧道配置、状态和控制接口。
- 增加登录相关环境变量和正式部署说明。
- 使用多阶段容器构建和锁定依赖版本，提升构建一致性。
- 更新前后端依赖并完成安全检查。
- 更新登录页面与隧道管理页面截图。

## 2026.8.2-zh-cn.5

- 修正仓库首页错误引用上游英文截图的问题。
- 统一界面、源码、镜像和文档中的版本号。

## 2026.8.2-zh-cn.4

- 首次公开发布中文定制版本。
- 集成 cloudflared 2026.8.2。
- 支持 `linux/amd64`、`linux/arm64` 和 `linux/arm/v7` 镜像。
